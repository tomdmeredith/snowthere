# Core Framework
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.26.0

# AI & Content
anthropic>=0.40.0

# Image Generation (3-tier fallback)
google-genai>=1.0.0     # Tier 1: Gemini (Flash/Pro models)
replicate>=0.25.0        # Tier 3: Flux Schnell

# Database
supabase>=2.3.0

# Research APIs
exa-py>=1.0.0
tavily-python>=0.3.0
# Note: Brave Search uses direct HTTP calls via httpx (no SDK needed)
# Replaced google-search-results (SerpAPI) with Brave - simpler, user already has key

# MCP Server (Model Context Protocol)
mcp>=1.0.0

# Google Search Console API
google-api-python-client>=2.0.0
google-auth>=2.0.0

# Environment
python-dotenv>=1.0.0

# Text Processing
unidecode>=1.3.0        # Transliterate non-Latin to ASCII for Google Places
beautifulsoup4>=4.12.0  # HTML parsing for official image scraping

# Scheduling
apscheduler>=3.10.0

# Dev dependencies (optional)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# ruff>=0.1.0
